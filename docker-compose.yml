services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.tvmaze.com}
    ports:
      - '8080:80'
    restart: unless-stopped
